-- Minimal configuration for script-runner.nvim
-- This shows the simplest possible setup

require("script-runner").setup({})

-- That's it! The plugin will use all default settings:
-- - Default keymaps enabled (<leader>sr, <leader>sR, etc.)
-- - Vertical terminal split
-- - Terminal reuse enabled
-- - Standard script filtering
